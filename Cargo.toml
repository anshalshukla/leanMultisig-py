[package]
name = "lean-multisig-py"
version = "0.1.0"
edition = "2021"

[lib]
name = "lean_multisig"
crate-type = ["cdylib"]

[features]
test-config = ["lean-multisig/test-config", "rec_aggregation/test-config", "leansig_wrapper/test-config"]

[dependencies]
pyo3 = { version = "0.27", features = ["extension-module"] }
lean-multisig = { git = "https://github.com/anshalshukla/leanMultisig", branch = "devnet4" }
rec_aggregation = { git = "https://github.com/anshalshukla/leanMultisig", branch = "devnet4" }
backend = { git = "https://github.com/anshalshukla/leanMultisig", branch = "devnet4" }
leansig_wrapper = { git = "https://github.com/anshalshukla/leanMultisig", branch = "devnet4" }
postcard = { version = "1.1.3", features = ["alloc"] }
serde = { version = "1.0", features = ["derive"] }
ssz = { package = "ethereum_ssz", version = "0.10.0" }

[build-dependencies]
serde_json = "1.0"
